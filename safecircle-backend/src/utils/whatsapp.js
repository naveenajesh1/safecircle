// utils/whatsapp.js
exports.sendEmergencyMessage = async (phoneNumber, location, media) => {
    console.log(`Sending WhatsApp message to ${phoneNumber} with location: ${location} and media: ${media}`);
};
